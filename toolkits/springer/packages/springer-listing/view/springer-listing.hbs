<ul class="c-listings">
	{{#each listings}}
	<li class="c-listings__item">
		<div class="c-listings__content">
			<h3 class="c-listings__title" {{#if ../level}} aria-level="{{../level}}" {{/if}}>
				<a href="{{permalink}}">{{title}} level:{{level}}</a>
			</h3>
			{{#if authors}}
			<div class="c-listings__authors">
				<span class="c-listings__authors-list"><span class="c-listings__authors-list">Authors:&nbsp;</span>{{#each authors}}<span>{{.}}</span>{{/each}}</span>
			</div>
			{{/if}}
			{{#if meta}}
			<div class="c-listings__metadata">
				{{#each meta}}<span>{{.}}</span>{{/each}}
			</div>
			{{/if}}
			{{#if options}}
			<ul class="c-listings__view-options">
				{{#each options}}
				<li>
					<a href="{{url}}">
						<svg class="c-listings__view-options-icon" width="16" height="16" aria-hidden="true">
							<use xlink:href="{{../../optionIcon}}"></use>
						</svg>
						<span>{{label}}</span>
					</a>
				</li>
				{{/each}}
			</ul>
			{{/if}}
		</div>
		{{#with image}}
		<div class="c-listings__image">
			<img src="{{imageSrc}}" alt="{{imageAlt}}">
		</div>
		{{/with}}
	</li>
	{{/each}}
</ul>
