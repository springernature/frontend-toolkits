<fieldset class="c-forms__fieldset"{{#unless legend}} role="presentation"{{/unless}}>
	{{#if legend}}
		<legend class="c-forms__legend">{{{legend}}}</legend>
	{{/if}}
	{{#each fields}}
		<div class="c-forms__field">
			{{> (lookup . 'type')}}
		</div>
	{{/each}}
</fieldset>
