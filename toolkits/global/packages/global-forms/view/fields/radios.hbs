<fieldset class="c-forms__field" {{#unless legend}} role="presentation"{{/unless}} {{> inputCommonAttributes}}>
	{{#if legend}}
		<legend class="c-forms__label">{{legend}}</legend>
	{{/if}}
	{{#if error}}
		{{> error}}
	{{/if}}
	{{#each inputs}}
		{{> radio }}
	{{/each}}
</fieldset>
