{{#if inputs}}
	<fieldset class="c-forms__fieldset c-forms__field c-forms__radios" {{#unless legend}} role="presentation"{{/unless}} {{> globalFormAttributes}}>
		{{#if legend}}
			<legend class="c-forms__label">{{legend}}</legend>
		{{else}}
			{{#if label}}
				<legend class="c-forms__label">{{label}}</legend>
			{{/if}}
		{{/if}}
		{{#if error}}
			{{> globalFormError}}
		{{/if}}

		{{#each inputs}}
			{{> globalFormRadio name=../name }}
		{{/each}}
	</fieldset>
{{/if}}
