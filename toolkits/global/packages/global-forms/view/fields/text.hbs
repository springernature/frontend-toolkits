{{> label}}
{{#if error}}
	{{> error}}
{{/if}}
<input type="text" class="c-forms-input c-forms-input--text"
	   {{> inputCommonAttributes}}
>
{{#if datalist.options}}
	{{#if datalist.id}}
		<datalist id="{{datalist.id}}">
			{{#each datalist.options}}
				<option value="{{.}}"></option>
			{{/each}}
		</datalist>
	{{/if}}
{{/if}}
