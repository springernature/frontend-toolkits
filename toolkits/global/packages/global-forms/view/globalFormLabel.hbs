<label for="{{id}}" class="c-forms__label">
	{{label}}{{#if required}}{{#if requiredSuffix}} {{{requiredSuffix}}}{{/if}}{{/if}}
	{{#if hint}}
		<small class="c-forms__hint">{{hint}}</small>
	{{/if}}
</label>
