<label for="{{id}}" class="c-forms__label">
	{{label}}
	{{#if optional}}
		{{#if optionalSuffix}}
			{{{optionalSuffix}}}
		{{else}}
			(optional)
		{{/if}}
	{{/if}}
	{{#if hint}}
		<small class="c-forms__hint">{{hint}}</small>
	{{/if}}
</label>
