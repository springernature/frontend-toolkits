<fieldset class="c-forms__fieldset">
	{{#if legend}}
		<legend class="c-forms__legend">{{{legend}}}</legend>
	{{/if}}
	{{#each fields}}
		<div class="c-forms__field">
			{{> (lookup . 'template')}}
		</div>
	{{/each}}
</fieldset>
