<{{#if legend}}fieldset{{else}}div{{/if}} class="c-forms__fieldset">
	{{#if legend}}
		<legend class="c-forms__legend">{{{legend}}}</legend>
	{{/if}}
	{{#each fields}}
		{{> globalFormField}}
	{{/each}}
</{{#if legend}}fieldset{{else}}div{{/if}}>
