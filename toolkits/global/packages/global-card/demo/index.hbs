<style>
	.demo-grid {
		list-style: none;
		padding: 0;
	}

	.demo-grid > * + * {
		margin-top: 1em;
		margin-top: var(--grid--gap, 1em);
	}


	@supports (display: grid) {
		.demo-grid > * {
			margin: 0;
		}

		.demo-grid {
			display: grid;
			gap: 1em;
			gap: var(--grid--gap, 1em);
			grid-template-columns: repeat(auto-fit, minmax(var(--grid--basis, 230px), 1fr));
		}
	}
</style>

<h2>Default</h2>

<p>The fourth card has <code>"noBlockLink": true</code> set because it contains a second link.</p>

<ul class="demo-grid">
{{#with cardsDefault}}
	{{#each cards}}
		{{>card}}
	{{/each}}
{{/with}}
</ul>

<h2>No shape (border, background, box-shadow)</h2>

<p>With the <code>noShape</code> Boolean applied to each card.</p>

<ul class="demo-grid">
{{#with cardsNoShape}}
	{{#each cards}}
		{{>card}}
	{{/each}}
{{/with}}
</ul>

<h2>No images</h2>

<ul class="demo-grid">
{{#with cardsNoImages}}
	{{#each cards}}
		{{>card}}
	{{/each}}
{{/with}}
</ul>

<h2>No images or shape</h2>

<ul class="demo-grid">
{{#with cardsNoImagesOrShape}}
	{{#each cards}}
		{{>card}}
	{{/each}}
{{/with}}
</ul>

<h2>No titles or shape</h2>

<ul class="demo-grid">
{{#with cardsNoTitles}}
	{{#each cards}}
		{{>card}}
	{{/each}}
{{/with}}
</ul>

<h2>Dark mode</h2>

<p>With the <code>dark</code> Boolean applied to each card.</p>

<ul class="demo-grid">
{{#with cardsDark}}
	{{#each cards}}
		{{>card}}
	{{/each}}
{{/with}}
</ul>
