<div class="c-footer">
	{{#if productGroups}}
		{{! Partials locations are set dynamically in the data }}
		{{> (lookup . 'productFooter')}}
	{{/if}}
	{{! Partials locations are set dynamically in the data }}
	{{> (lookup . 'corporateFooter')}}
</div>
