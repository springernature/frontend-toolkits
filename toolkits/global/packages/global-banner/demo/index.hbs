<div class="c-banner {{#if isInverted}}c-banner--inverted{{/if}} {{#if isFlush}}c-banner--flush{{/if}}">
    <div class="c-banner__container">
		{{#each items}}
			<div class="c-banner__item">
				{{{this}}}
			</div>
		{{/each}}
	</div>
</div>
